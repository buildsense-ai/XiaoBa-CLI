// Stable V3 entry point. Keep the implementation in one module so callers
// can use either the descriptive or versioned import without creating a
// second transition writer.
export * from './skill-evolution';
